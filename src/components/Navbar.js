import React from "react";

const Navbar = () => {
    return(
        <nav>
            <button> Planetas</button>
            <button> Personas</button>
        </nav>
    );
}

export default Navbar;